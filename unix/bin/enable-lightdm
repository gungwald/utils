#!/bin/sh

for svc in gdm.service lxdm.service kdm.service; do
    if sudo systemctl --quiet is-enabled $svc; then
        sudo systemctl disable $svc
    fi
done
sudo systemctl enable lightdm.service
