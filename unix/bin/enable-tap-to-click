#!/bin/sh

echo Read the contents of this file for instructions.

# This might be required for Dell Precision M6300, not sure.
# Requires packages to be installed:
#
#   Fedora-based:
#       sudo dnf install xorg-x11-drv-synaptics
#
#   Debian-based:
#       sudo apt install xserver-xorg-input-synaptics
#
#   Then restart the X server
#

# This doesn't work for Dell Precision M6300, but apparently does for something.
#synclient TapButton1=1

# The other option is libinput, configured like this in xorg.conf.d/40-touchpad.conf
# This is the only option that works for the Dell Precision M6300.
# Section "InputClass"
#   Identifier "touchpad"
#   Driver "libinput"
#   MatchIsTouchpad "on"
#   Option "Tapping" "on"
#   Option "NaturalScrolling" "on"
#   Option "ClickMethod" "clickfinger"
# EndSection

# This is not needed for Dell Precision M6300, but apparently was for something.
#xinput set-prop "SynPS/2 Synaptics TouchPad" 
