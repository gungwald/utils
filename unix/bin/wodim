#!/bin/bash

UID=`id -u`
if [ "$UID" -eq 0 ]
then
	echo Detected user root. This is required to elevate priority for real-time process.
	ELEVATE=
else
	echo Running as root is necessary to increase priority.
	ELEVATE=sudo
fi

"$ELEVATE" /usr/bin/wodim "$*"

